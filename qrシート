Option Explicit

Private Sub Worksheet_Change(ByVal Target As Range)

    On Error GoTo SafeExit

    If Intersect(Target, Me.Range("C13")) Is Nothing Then Exit Sub

    Dim qr As String
    qr = CStr(Me.Range("C13").Value)

    If Trim(qr) = "" Then Exit Sub

    Application.EnableEvents = False
    Me.Range("C13").MergeArea.ClearContents
    Application.EnableEvents = True

    QR_Process qr

    Exit Sub

SafeExit:
    Application.EnableEvents = True
    MsgBox "QR読込処理でエラーが発生しました。" & vbCrLf & Err.Description, vbExclamation

End Sub

Private Sub Worksheet_Activate()

    QR_ApplyScreenProtection
    QR_FocusInput

End Sub